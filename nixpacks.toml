# WeasyPrint system libraries for match prediction photos.
# "..." keeps auto-detected packages (including Python) from the provider.
[phases.setup]
nixPkgs = [
  "...",
  "cairo",
  "pango",
  "gdk-pixbuf",
  "glib",
  "harfbuzz",
  "fribidi",
  "libffi",
  "zlib",
  "fontconfig",
  "freetype",
  "libpng",
]
nixLibs = ["...", "cairo", "pango", "gdk-pixbuf"]
